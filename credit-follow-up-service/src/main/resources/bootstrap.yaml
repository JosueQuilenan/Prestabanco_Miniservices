spring:
  application:
    name: credit-follow-up-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/prestabanco_credit-follow-ups
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update