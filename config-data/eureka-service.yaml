server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server-service
    prefer-ip-address: false
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url: 
      default-zone: http://eureka-server-service:${server.port}/eureka/