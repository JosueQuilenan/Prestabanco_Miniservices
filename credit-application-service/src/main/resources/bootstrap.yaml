spring:
  application:
    name: credit-application-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/prestabanco_credit-applications
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
